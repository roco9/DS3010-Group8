if running locally you must create a .env file in the backend folder with MONGODB_URL="mongodb url"

the model must also be saved from the jupyter notebook and moved into the website/backend/ folder (it is too large to store or push to github)